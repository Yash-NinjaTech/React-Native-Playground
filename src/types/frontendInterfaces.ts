export interface GlobalChatAnswersState {
  destination: string;
  dates: string;
  partySize: string;
}

export interface SearchActivitiesState {
  startDate: string;
  endDate: string;
  startLocation: string;
}
